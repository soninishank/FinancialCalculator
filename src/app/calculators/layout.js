import Footer from '../../components/common/Footer';

export default function CalculatorLayout({ children }) {
    return (
        <>
            {children}
            <Footer />
        </>
    );
}
