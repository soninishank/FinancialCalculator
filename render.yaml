services:
  # Backend Service
  - type: web
    name: ipo-investment-calc-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start:server
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_VERSION
        value: 20.10.0
    autoDeploy: false
